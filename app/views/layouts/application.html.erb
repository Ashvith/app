<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <div class="container">
      <br>
      <h6 class="text-right">Currently signed in as: <%= current_user.present? ? current_user.email : 'None' %></h6>
    </div>
    <%= yield %>
    <div class="container">
      <p class="notice text-center font-weight-bold" id="notice"><%= notice %></p>
      <p class="alert text-center font-weight-bold" id="alert"><%= alert %></p>
    </div>
  </body>
</html>
